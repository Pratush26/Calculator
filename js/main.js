// Add all your JS here
let string = ""
let buttons = document.querySelectorAll('.btn')

console.log(Array.from(".btn"))
/*Array.from('buttons').forEach((b)=>{
  b.AddEventLisener('click',(e)=>{
    if(e.target.InnerHTML =="="){
      string = eval(string)
    }
  })
)}*/